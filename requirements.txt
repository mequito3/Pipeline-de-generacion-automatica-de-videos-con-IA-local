# ─── Shorts Factory — requirements.txt ──────────────────────────────────────
# Instalación: pip install -r requirements.txt
# Python 3.10+ requerido

# ─── LLM ─────────────────────────────────────────────────────────────────────
ollama>=0.6.1                   # Cliente Ollama (llama3, mistral, etc.)
httpx>=0.28.0                   # HTTP async — llamadas a Groq API

# ─── TTS ──────────────────────────────────────────────────────────────────────
edge-tts>=7.0.0                 # Microsoft Edge TTS neural (gratis, requiere internet)
pyttsx3>=2.90                   # TTS offline con voces del sistema (fallback)
pydub>=0.25.1                   # Conversión y mezcla de audio (requiere ffmpeg en PATH)

# ─── Subtítulos sincronizados ────────────────────────────────────────────────
faster-whisper>=1.2.0           # Transcripción rápida con CTranslate2
stable-ts>=2.19.0               # Forced alignment palabra a palabra
openai-whisper>=20231117        # Modelo base requerido por stable-ts

# ─── Imágenes ─────────────────────────────────────────────────────────────────
Pillow>=12.0.0                  # Procesamiento de imágenes, thumbnails, overlays

# ─── Scraping de historias ────────────────────────────────────────────────────
beautifulsoup4>=4.12.0          # Parsing HTML para scraper de confesiones
requests>=2.31.0                # HTTP síncrono — Pexels, SD, scraping

# ─── Upload a YouTube ─────────────────────────────────────────────────────────
nodriver>=0.48.0                # Automatización Chrome sin detección (reemplaza Selenium)

# ─── Notificaciones WhatsApp ──────────────────────────────────────────────────
twilio>=9.0.0                   # API de WhatsApp vía Twilio sandbox

# ─── Utilidades ───────────────────────────────────────────────────────────────
numpy>=1.22.0                   # Arrays numéricos (dependencia de whisper)
python-dotenv>=1.0.0            # Cargar .env con credenciales
json5>=0.9.0                    # Parser JSON permisivo (fallback para output del LLM)

# ─── Dependencias del sistema (NO pip) — instalar manualmente ─────────────────
# ffmpeg  : winget install ffmpeg          (requerido para video y audio)
# Chrome  : https://www.google.com/chrome/ (requerido por nodriver)
# Ollama  : https://ollama.ai              (servidor LLM local)
